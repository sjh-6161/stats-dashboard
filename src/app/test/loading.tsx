import { LoadingSection } from "@/components/ui/loading-section"

export default function Loading() {
  return (
    <LoadingSection className="h-full min-h-[600px]" message="Loading map visualization..." />
  )
}
